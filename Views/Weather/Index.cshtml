@model WeatherDashboard.Models.WeatherResponse

@{
    ViewData["Title"] = "Weather Dashboard";
}

<div class="container mt-5">
    <form asp-action="Index" method="get" class="mb-4">
        <div class="input-group">
            <input type="text" name="city" class="form-control" placeholder="Enter city name..." value="@Model.Name" />
            <button type="submit" class="btn btn-primary">Search</button>
        </div>
    </form>

    <h1 class="mb-4">Weather Overview for @Model.Name</h1>
    <div class="card shadow-sm">
        <div class="card-body">
            <h3>@Model.Weather[0].Main - @Model.Weather[0].Description</h3>
            <p>🌡️ Temperature: @Model.Main.Temp°C (Feels like @Model.Main.FeelsLike°C)</p>
            <p>💧 Humidity: @Model.Main.Humidity%</p>
            <p>🌬️ Wind Speed: @Model.Wind.Speed m/s</p>
            <p>☁️ Cloudiness: @Model.Clouds.All%</p>
            <p>📍 Location: Latitude @Model.Coord.Lat, Longitude @Model.Coord.Lon</p>
        </div>
    </div>
</div>
